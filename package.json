{
  "name": "idbi-msme-financial-health-card",
  "private": true,
  "description": "IDBI MSME Financial Health Card — Team Anvay. Vercel hosts the UI + Express API; ML service is optional via ML_SERVICE_URL.",
  "scripts": {
    "vercel-build": "npm run build --prefix backend && npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "@vercel/node": "^5.3.6"
  },
  "engines": {
    "node": ">=18"
  }
}
